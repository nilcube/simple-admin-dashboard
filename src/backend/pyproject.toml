[project]
name = "backend"
version = "0.0.1"
description = "simple backend for the app"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "mysql-connector-python>=26.7.0",
    "pydantic>=2.13.5",
]
